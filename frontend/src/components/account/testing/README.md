# 账号模型批量测试

从历史提交 `7e5352a9a` 恢复逐模型测试并清理失败映射。测试使用已有 SSE 接口，全部失败、取消、网络错误或不完整响应不会提交配置。保存前读取最新凭证，保留 Base URL、认证设置、未测试映射与成功模型的别名目标。个人账号测试复用同一 SSE 完成判定。

收到明确的完成事件后立即结束读取，不等待连接关闭。单模型和批量测试共用 120 秒超时及取消逻辑；批量等待时显示当前模型并提供取消按钮。测试模型列表加载限定 10 秒，失败显示重试入口，关闭或切换账号会取消旧请求。
